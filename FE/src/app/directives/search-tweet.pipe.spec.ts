import { SearchTweetPipe } from './search-tweet.pipe';

describe('SearchTweetPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchTweetPipe();
    expect(pipe).toBeTruthy();
  });
});
